import styled from 'styled-components'
import {
  color,
  display,
  fontSize,
  fontWeight,
  lineHeight,
  maxWidth,
  space,
  textAlign,
  width
} from 'styled-system'

const Text = styled.div`
	${color}
	${display}
	${fontSize}
	${fontWeight}
	${lineHeight}
	${maxWidth}
	${space}
	${textAlign}
	${width}
`

export default Text
