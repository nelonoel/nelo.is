import styled from 'styled-components'
import {
	color,
	display,
	fontSize,
	fontWeight,
	lineHeight,
	space,
	textAlign,
	width
} from 'styled-system'

const Text = styled.div`
	${color}
	${display}
	${fontSize}
	${fontWeight}
	${lineHeight}
	${space}
	${textAlign}
	${width}
`

export default Text
