import styled from 'styled-components'
import {
	display,
	flexDirection,
	flexWrap,
	lineHeight,
	position,
	space,
	textAlign,
	width
} from 'styled-system'

const Box = styled.div`
	${display}
	${flexDirection}
	${flexWrap}
	${lineHeight}
	${position}
	${space}
	${textAlign}
	${width}
`

export default Box
